<% if @subscription && @subscription.destroyed? %>
  $('.subscription').html("<%= j render partial: 'subscriptions/link', locals: { subscriptable: @subscription.subscriptable } %>");
<% else %>
  console.log('Subscribe destroy error')
<% end %>
